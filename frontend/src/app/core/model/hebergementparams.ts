export class HebergementParams {
   
    frontend:number;
    backend:number;
    db:number;
    noueds:number;
    nom:string;

constructor(frontend: number, backend: number, db: number, noueds: number, nom: string) {
      
      
      this.frontend=frontend;
      this.backend=backend;
      this.db=db;
      this.noueds=noueds;
      this.nom=nom;








    }
  }
  